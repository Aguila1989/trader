/**
 * The support mailbox, shared by every "contact us" surface (sidebar help,
 * global footer, transparency page).
 *
 * PLACEHOLDER ADDRESS — the operator MUST replace this with a real, monitored
 * support mailbox before launch (there is currently no other way for a user to
 * reach anyone). Tracked in DEPLOY.md.
 */
export const SUPPORT_EMAIL = "support@example.com";
